apiVersion: v1
kind: Service
metadata:
  name: {{ include "blotto.fullname" . }}-backend
  labels:
    {{- include "blotto.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "blotto.backend.selectorLabels" . | nindent 4 }}
