apiVersion: v1
kind: Service
metadata:
  name: {{ include "nasharena.fullname" . }}-backend
  labels:
    {{- include "nasharena.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nasharena.backend.selectorLabels" . | nindent 4 }}
