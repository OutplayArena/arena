apiVersion: v1
kind: Service
metadata:
  name: {{ include "nasharena.fullname" . }}-redis
  labels:
    {{- include "nasharena.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.redis.port }}
      targetPort: redis
      protocol: TCP
      name: redis
  selector:
    {{- include "nasharena.redis.selectorLabels" . | nindent 4 }}
