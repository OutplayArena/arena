apiVersion: v1
kind: Service
metadata:
  name: {{ include "nasharena.fullname" . }}-docs
  labels:
    {{- include "nasharena.labels" . | nindent 4 }}
    app.kubernetes.io/component: docs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nasharena.docs.selectorLabels" . | nindent 4 }}
