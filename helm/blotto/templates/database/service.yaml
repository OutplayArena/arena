apiVersion: v1
kind: Service
metadata:
  name: {{ include "blotto.fullname" . }}-db
  labels:
    {{- include "blotto.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgresql
      protocol: TCP
      name: postgresql
  selector:
    {{- include "blotto.database.selectorLabels" . | nindent 4 }}
