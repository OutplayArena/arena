apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "arena.fullname" . }}-backend
  labels:
    {{- include "arena.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      {{- include "arena.backend.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "arena.backend.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/oauth-secret: {{ .Values.oauth | toJson | sha256sum }}
        checksum/config: {{ dict
            "apiPrefix"            .Values.backend.env.apiPrefix
            "enableAgentRestApi"   (default "true" .Values.backend.env.enableAgentRestApi)
            "enableAdminDashboard" (default "false" .Values.backend.env.enableAdminDashboard)
            "mcpPublicBaseUrl"     .Values.mcpServer.publicBaseUrl
          | toJson | sha256sum }}
    spec:
      serviceAccountName: {{ include "arena.fullname" . }}-backend
      containers:
        - name: backend
          image: "{{ .Values.backend.image.repository }}:{{ .Values.backend.image.tag }}"
          imagePullPolicy: {{ .Values.backend.image.pullPolicy }}
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: {{ include "arena.fullname" . }}-backend
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "arena.fullname" . }}-db
                  key: password
            - name: DATABASE_URL
              value: "postgresql+asyncpg://{{ .Values.database.user }}:$(DB_PASSWORD)@{{ include "arena.fullname" . }}-db:5432/{{ .Values.database.database }}"
            - name: OAUTH_CALLBACK_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ include "arena.fullname" . }}-oauth
                  key: callback-base-url
            - name: GITHUB_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "arena.fullname" . }}-oauth
                  key: github-client-id
            - name: GITHUB_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "arena.fullname" . }}-oauth
                  key: github-client-secret
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: {{ include "arena.fullname" . }}-oauth
                  key: google-client-id
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "arena.fullname" . }}-oauth
                  key: google-client-secret
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "arena.fullname" . }}-oauth
                  key: jwt-secret
            - name: REDIS_URL
              value: "redis://{{ include "arena.fullname" . }}-redis:{{ .Values.redis.port }}/0"
          livenessProbe:
            httpGet:
              path: {{ .Values.backend.env.apiPrefix }}/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: {{ .Values.backend.env.apiPrefix }}/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.backend.resources | nindent 12 }}
{{- if .Values.dev.liveReload }}
          volumeMounts:
            - name: static-files
              mountPath: /app/static
{{- end }}
{{- if .Values.dev.liveReload }}
      volumes:
        - name: static-files
          hostPath:
            path: {{ .Values.dev.staticSourcePath | quote }}
            type: Directory
{{- end }}
