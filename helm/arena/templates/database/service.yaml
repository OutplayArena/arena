apiVersion: v1
kind: Service
metadata:
  name: {{ include "arena.fullname" . }}-db
  labels:
    {{- include "arena.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgresql
      protocol: TCP
      name: postgresql
  selector:
    {{- include "arena.database.selectorLabels" . | nindent 4 }}
