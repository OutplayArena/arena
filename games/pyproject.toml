[project]
name = "games"
version = "0.1.0"
description = "Game implementations for OutplayArena"
requires-python = ">=3.10"
dependencies = [
    "outplayarena-sdk",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["games"]

[tool.uv.sources]
outplayarena-sdk = { path = "../agent-sdk", editable = true }
